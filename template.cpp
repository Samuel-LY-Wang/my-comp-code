// Imports
#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
#include <map>
#include <unordered_set>
#include <string>
#include <numeric>

// Data types
#define ll long long;
#define ld long double;
#define str string;
#define dict map;
#define vec vector;
#define set unordered_set;
#define multiset unordered_multiset;

// Macros
#define NOTPOSS cout << -1;
#define YES cout << "YES";
#define NO cout << "NO";
#define line_end(i, t) if (i != t-1) cout << endl;
#define print(arr, n) for (int i = 0; i < n; i++) cout << arr[i] << " ";
#define all(x) x.begin(), x.end();

using namespace std;

void solve() {

}

int main() {
    int t;
    cin >> t;
    while(t--) {
        solve();
    }
    return 0;
}